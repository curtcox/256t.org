name = "256t-org-site"
main = "worker.js"
compatibility_date = "2024-01-01"

# Bind the R2 bucket to the worker
[[r2_buckets]]
binding = "BUCKET"
bucket_name = "256t-cids"

# Routes for the custom domain
routes = [
  { pattern = "256t.org/*", zone_name = "256t.org" }
]
